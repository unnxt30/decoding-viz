// Distribution panel render. Agent implements in Task B2 (needs strategies.js).
// Responsibility: bars for the selected node's top-M; top-p nucleus highlighted, top-k cutoff
// line, entropy / kept-count / "other" mass readout; live re-render when sliders change.

export function renderDistribution(container, metaEl, data, nodeId, params) {
  // TODO(agent, Task B2): compute trueProbs(node), apply params (temp/k/p), draw bars + cutoffs.
}
