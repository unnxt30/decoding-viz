// Token-tree SVG render. Agent implements in Task B1.
// Responsibility: draw the flat node map as a tree — current path highlighted, unexplored
// muted, fork nodes (high entropy) ringed amber, node size ∝ probability; click → onSelect(id).

export function renderTree(svg, data, selectedId, onSelect) {
  // TODO(agent, Task B1): lay out nodes by depth, draw edges + nodes, wire click -> onSelect.
}
