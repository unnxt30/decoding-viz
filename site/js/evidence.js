// Evidence mode. Agent implements in Task B3.
// Responsibility: render each strategy's continuation side by side from the stored paths
// (greedy_path, beam_paths) + a couple of sampled rollouts — the screenshot-able payoff.

export function renderEvidence(container, data) {
  // TODO(agent, Task B3): render greedy / beam / top-k / top-p / high-temp continuations as text rows.
}
